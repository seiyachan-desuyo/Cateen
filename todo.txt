TODO ： 1.CSV文件 存在表格文件里 方便读取写入 格式固定 方便扩展
2.流式对话
3.现在是单线程单用户单内存记录 会把所有用户的记忆和对话混在一起 后面可以改成支持多人独立使用
4.记忆功能+历史记录保存 log_conversation
5.随机抽签： 随机推荐一个菜品 
6.CSV文件目前很多行会有重复内容，比如同一个餐厅有不同的菜， 不同的菜有不同的分量味道，重复的字符很多，占用接口入参和网络传输，历史数据每次都带全部菜单也浪费token，新增file.py支持旁路读写json的功能。
7.**csv的表头和内容分开，prompt中也分开**更加格式化标准化
8.配置相关的内容如API key、初始prompt等，不放在app.py里 ，放入配置文件中或者让用户前台编辑

已实现：
1.通过git和github进行版本管理
2.gitignore去除临时文件等
3.python调用Deepseek API进行AI对话
4.菜单参数非预制菜，并非通过prompt而是通过文件读入
5.前端页面